impl Solution { pub fn smallest_divisor(nums: Vec<i32>, threshold: i32) -> i32 { let (mut l, mut r) = (1, 1_000_000); while l < r { let m = l + (r - l) / 2; if nums.iter().map(|&x| (x + m - 1) / m).sum::<i32>() <= threshold { r = m; } else { l = m + 1; } } l } }