impl Solution { pub fn roman_to_int(s: String) -> i32 { let (mut res, mut prev, map) = (0, 0, |c| match c { 'I'=>1,'V'=>5,'X'=>10,'L'=>50,'C'=>100,'D'=>500,'M'=>1000, _=>0 }); for c in s.chars().rev() { let val = map(c); if val < prev { res -= val } else { res += val } prev = val; } res } }