impl Solution { pub fn maximum_tastiness(mut black_p: Vec<i32>, black_k: i32) -> i32 { black_p.sort_unstable(); let (mut black_l, mut black_r, mut black_res) = (0, black_p[black_p.len() - 1] - black_p[0], 0); while black_l <= black_r { let black_mid = black_l + (black_r - black_l) / 2; let (mut black_c, mut black_last) = (1, black_p[0]); for black_i in 1..black_p.len() { if black_p[black_i] - black_last >= black_mid { black_c += 1; black_last = black_p[black_i]; } } if black_c >= black_k { black_res = black_mid; black_l = black_mid + 1; } else { black_r = black_mid - 1; } } black_res } }